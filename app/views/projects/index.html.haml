%h1 Listing Projects
.field
  .clear
%table.listing
  %tr
    %th Name
    %th Description
    %th Estimated budget
    %th Hourly rate
    %th Estimated hour
    %th Start Date
    %th End Date
    %th Manager
    %th Client
    %th
    %th{:width=>"40px"}
    

  - @projects.each do |project|
    %tr
      %td= link_to project.name,project
      %td= project.description
      %td= project.estimated_budget
      %td= project.hourly_rate
      %td= project.estimated_hour
      %td= project.start_date
      %td= project.end_date
      %td= project.manager.try(:email)
      %td= project.client.try(:name)
      %td= link_to 'Add Task', new_task_path(:project => project)
      %td.edit_link 
        = link_to image_tag('edit_10.png',:border => 0), edit_project_path(project)
        = link_to image_tag('delete_10.png',:border => 0), project, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Project', new_project_path
